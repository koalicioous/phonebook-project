/** @jsxImportSource @emotion/react */
"use client";
import { pageContainerStyle } from "@/styles/SharedCSS";

const CreateContactPage = () => {
  return <main css={pageContainerStyle}>Create Contact Page</main>;
};

export default CreateContactPage;
